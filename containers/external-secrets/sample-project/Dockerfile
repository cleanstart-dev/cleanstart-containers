FROM cleanstart/external-secrets:latest-dev

# Simple test image based on CleanStart External Secrets
# The external-secrets binary is already available at /usr/bin/external-secrets

# You can customize the default command if needed
# CMD ["version"]
